<%= form_for(@belief) do |f| %>
  <% if @belief.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@belief.errors.count, "error") %> prohibited this belief from being saved:</h2>

      <ul>
      <% @belief.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
